name: E2E Tests (v10)

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  e2e-test:
    name: E2E Tests (v10) on Ubuntu
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: v10/app

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: |
            v10/lib/package-lock.json
            v10/app/package-lock.json

      - name: Build morse-engine (lib)
        working-directory: v10/lib
        run: |
          npm ci
          npm run build

      - name: Install app dependencies
        run: npm ci

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('v10/app/package-lock.json') }}

      - name: Install Playwright browsers (full)
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install chromium --with-deps

      - name: Install Playwright dependencies only
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: npx playwright install-deps chromium

      - name: Run debug E2E tests
        run: npx playwright test e2e/debug.spec.ts --project=chromium
        continue-on-error: true

      - name: Run all E2E tests
        run: npm run test:e2e

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: v10/app/playwright-report/
          retention-days: 30
